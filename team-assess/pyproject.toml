[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "team-assess"
version = "0.8.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.34.0",
    "pyyaml>=6.0",
    "pypdf>=4.0",
]

[project.scripts]
team-assess = "assess:main"
